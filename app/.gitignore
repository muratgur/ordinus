# App-local ignores are mirrored in the repository root .gitignore.
node_modules/
dist/
out/
.DS_Store
.eslintcache
*.log*
# Bundled Node runtime (ADR-047): downloaded at packaging time, never committed.
resources/runtime/
